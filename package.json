{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Web assets ready'",
    "ionic:build": "npm run build",
    "appflow:build": "npm run build && (npx cap add android || echo 'Already added')",
    "capacitor:sync:before": "npx cap add android || echo 'Android already exists'"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
